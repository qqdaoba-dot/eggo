# eggo Circular Screen Prototype

Static prototype for the eggo circular screen interaction.

- `index.html` is the prototype entry.
- `assets_cutout/` contains transparent character PNGs used by the UI.
